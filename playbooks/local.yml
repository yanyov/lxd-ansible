- name: Configure our localhost
  hosts: localhost
  remote_user: root
  gather_facts: true
  roles:
    - role: openssl
    # - role: jenkins
    #- role: rabbitmq
    #  vars:
    #    rabbitmq_install: false
